import { AccountEntity } from './account.entity';
import { BalanceEntity } from './balance.entity';
import { TransactionEntity } from './transaction.entity';

export const entities = [AccountEntity, BalanceEntity, TransactionEntity];
