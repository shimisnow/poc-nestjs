/** Information about the created and linked transactions */
export class CreatePairTransactionResult {
  /** Id for the created transaction with data from the 'from' input  */
  fromTransactionId: number;
  /** Id for the created transaction with data from the 'to' input */
  toTransactionId: number;
}
